{
  "tabs": [
    {
      "id": "general",
      "label": "General",
      "fields": [
        {
          "key": "trial_days",
          "label": "Default Trial Days",
          "component": "input",
          "inputType": "number",
          "min": 0,
          "max": 90,
          "description": "Default trial period in days (0 = no trial)"
        },
        {
          "key": "max_subscriptions_per_user",
          "label": "Max Subscriptions per User",
          "component": "input",
          "inputType": "number",
          "min": 1,
          "max": 100,
          "description": "Maximum concurrent subscriptions a user can hold"
        },
        {
          "key": "allow_downgrade",
          "label": "Allow Downgrade",
          "component": "checkbox",
          "description": "Allow users to downgrade to a cheaper plan"
        },
        {
          "key": "proration_enabled",
          "label": "Enable Proration",
          "component": "checkbox",
          "description": "Calculate prorated credits on plan changes"
        }
      ]
    },
    {
      "id": "lifecycle",
      "label": "Lifecycle",
      "fields": [
        {
          "key": "expiration_check_interval_seconds",
          "label": "Expiration Check Interval (seconds)",
          "component": "input",
          "inputType": "number",
          "min": 30,
          "max": 3600,
          "description": "How often to check for expired subscriptions"
        },
        {
          "key": "dunning_intervals_days",
          "label": "Dunning Intervals (days, comma-separated)",
          "component": "input",
          "inputType": "text",
          "description": "Days after payment failure to send reminders (e.g., 3,7)"
        }
      ]
    }
  ]
}
